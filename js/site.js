/*
 * Social
 * @see _includes/social-markup.html
 * @selector .js-social
 */
;( function( win, doc ) {

  $q = doc.querySelectorAll;
  getId = doc.getElementById;

  if ( 'function' === typeof $q ) {

	// do you, bae

  } else {
	console.log( 'TODO' );
  }

} )( this, this.document );
